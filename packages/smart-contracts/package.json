{
  "name": "smart-contracts",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "hardhat compile && tsup typechain-types/index.ts --format esm,cjs --dts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hardhat": "^2.19.4",
    "ts-node": "^10.9.2",
    "tsup": "^8.0.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  }
}
